<h2><%= board.get('title') %></h2>

<ul>

<% board_lists.each(function(list){ %>

	<li>
	<a href="#" class="list_link" data-id="<%= list.id %>">
		<%= list.get('title') %>
	</a>
	</li>

<% }); %>

</ul>
